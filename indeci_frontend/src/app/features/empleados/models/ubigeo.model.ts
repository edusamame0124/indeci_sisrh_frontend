/** Alineado a `UbigeoDto`; `id` es código distrito para `distritoId`. */
export interface UbigeoOption {
  readonly id: string;
  readonly distrito: string;
  readonly provincia: string;
  readonly departamento: string;
}
